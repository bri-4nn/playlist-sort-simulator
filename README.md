# playlist-sort-simulator
A small cpp program that simulates a music player such a streaming music service does it. It uses a queue structure to add any song and sorts them with different sort methods.
